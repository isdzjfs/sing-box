# sing-box 项目工作约定

## 同步上游与本地修复保护

- 执行“同步上游更改”、合并上游或重构 URLTest 相关路径前，必须先阅读 [本地行为保护清单](docs/maintenance/UPSTREAM_SYNC_GUARDS.md)。从其他仓库进入本仓库操作时也适用。
- 每次同步都检查上游是否触碰清单中的实现、调用方、接口、依赖或测试；Git 无文本冲突不代表行为兼容。上游历史重写时先审查补丁等价性和真实增量。
- 默认保留已修复的本地行为。只有源代码比较与回归测试证明上游实现等价或更好，且未丢失本地能力时，才采用上游实现；在合并说明中记录证据。不要机械使用 `ours`/`theirs` 解决语义冲突。
- 保护的是行为约束，不是固定代码、函数名或提交哈希。重构可以改变实现，但不能通过删除或削弱回归测试来放行退化。
- 按清单执行有针对性的验证；涉及并发状态时执行竞态检测，涉及 libbox/daemon 协议时核对 SenVPN 消费端。测试或环境限制必须如实说明。
- 后续修复同类问题时同步更新清单中的行为、代码入口和回归测试；被更好实现替代的条目保留替代依据。

## 编辑与提交

- 保留与当前任务无关的工作区改动；仅暂存本任务明确涉及的文件。
- 代码在必要处添加注释，说明需要保持的行为与原因。
- Codex 参与创建提交时，在提交信息末尾空一行追加：

  `Co-authored-by: chatgpt-codex-connector[bot] <199175422+chatgpt-codex-connector[bot]@users.noreply.github.com>`
